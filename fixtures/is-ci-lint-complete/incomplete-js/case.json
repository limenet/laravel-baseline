{
    "check": "isCiLintComplete",
    "description": "fails when the project has TypeScript but the ci-lint npm script skips the type checker",
    "engines": [
        "js"
    ],
    "expect": "fail"
}
