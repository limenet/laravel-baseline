{
    "check": "nodeVersion",
    "description": "fails when engines.node is absent, and adopts the .nvmrc major on fix",
    "engines": ["php", "js"],
    "expect": "fail",
    "fix": {
        "expect": "pass",
        "json": { "package.json": { "engines.node": "^26" } }
    }
}
