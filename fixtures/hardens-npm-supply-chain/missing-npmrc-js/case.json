{
    "check": "hardensNpmSupplyChain",
    "description": "fails without an .npmrc, and writes both hardening keys plus the baseline's cooldown exclude on fix",
    "engines": ["js"],
    "expect": "fail",
    "fix": {
        "expect": "pass",
        "files": { ".npmrc": "engine-strict=true\nmin-release-age=7\nmin-release-age-exclude[]=@limenet-ch/baseline\n" }
    }
}
