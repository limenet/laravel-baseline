{
    "check": "ciSetsNodeVersion",
    "description": "fails when NODE_VERSION pins a specific major instead of \"latest\"",
    "engines": [
        "js"
    ],
    "expect": "fail",
    "fix": {
        "expect": "pass"
    }
}
