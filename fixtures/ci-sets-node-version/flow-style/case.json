{
    "check": "ciSetsNodeVersion",
    "description": "adds the variable to a mapping written in flow style",
    "engines": [
        "php",
        "js"
    ],
    "expect": "fail",
    "fix": {
        "expect": "pass"
    }
}
