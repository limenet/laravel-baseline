{
    "check": "ciSetsNodeVersion",
    "description": "adds the variable to a mapping whose header carries a trailing comment",
    "engines": [
        "js"
    ],
    "expect": "fail",
    "fix": {
        "expect": "pass"
    }
}
