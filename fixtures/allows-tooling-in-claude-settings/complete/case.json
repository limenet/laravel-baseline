{
    "check": "allowsToolingInClaudeSettings",
    "description": "passes when both the shared and php allow entries are present",
    "engines": [
        "php",
        "js"
    ],
    "expect": "pass"
}
